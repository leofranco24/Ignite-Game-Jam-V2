using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shipscrit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        t
        //gameObject.name;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
